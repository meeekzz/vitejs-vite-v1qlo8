import { useState } from 'react';
import './';
import CalculatorForm from './CalculatorForm';

function ColdLiquorTank() {
  const [count, setCount] = useState(0);

  return <CalculatorForm />;
}

export default ColdLiquorTank;
